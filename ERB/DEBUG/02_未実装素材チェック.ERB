@DEBUG_NAME_2
RESULTS = �������f�ރ`�F�b�N

@DEBUG_2
#DIMS ����
DRAWLINE
PRINTL �������f��
DRAWLINE
���� = 
FOR LOCAL, 0, 2000
	TRYCCALLFORM ITEM_RECIPE_{LOCAL}
		SPLIT RESULTS, "/", LOCALS
		FOR LOCAL:1, 1, RESULT
			SIF STRCOUNT(LOCALS:(LOCAL:1), "�y")
				CONTINUE
			SIF GET_NO("�A�C�e��", AUTO_SPLIT(LOCALS:(LOCAL:1), "*")) >= 0
				CONTINUE
			SIF STRCOUNT(����, AUTO_SPLIT(LOCALS:(LOCAL:1), "*"))
				CONTINUE
			PRINTFORML %AUTO_SPLIT(LOCALS:(LOCAL:1), "*")%
			���� += AUTO_SPLIT(LOCALS:(LOCAL:1), "*")
		NEXT
	CATCH
	ENDCATCH
NEXT
WAITANYKEY
