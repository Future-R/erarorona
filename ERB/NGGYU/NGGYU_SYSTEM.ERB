@存放帧, 帧文本
#DIMS REF 帧文本
#LOCALSIZE 1

CLEARLINE LINECOUNT
PRINTL [右键跳过动画] 
PRINTL
HTML_PRINT 帧文本
; 底部空行，把画面垫高
FOR LOCAL, 0, 底部空行数
	PRINTL
NEXT
帧文本 = 
帧计数器++
; 用 REDRAW 2（命令执行时立即强制刷新）逐帧渲染。
; 这里每帧都会 CLEARLINE LINECOUNT 大清屏再重绘，如果用 REDRAW 1（按帧率主动刷新），
; 引擎会在帧率定时器上异步重绘，可能正好撞上"已清屏、新帧还没画完"的瞬间而闪黑屏；
; 设置里的帧率越高撞得越频繁（>3 帧开始明显闪烁）。
; 改为同步强制刷新后，画面只在帧内容画完时更新，与帧率设置无关。
REDRAW 2
TWAIT 播放延迟, 0

; 每50帧 yield（原版每200帧 AWAIT）
SIF !(帧计数器 % 50)
	AWAIT

; 右键退出（MESSKIP 在渲染后检查）
SIF MESSKIP()
	帧计数器 = -9999