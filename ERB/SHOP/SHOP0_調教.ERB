@SHOPCOMNAME_0
RESULTS = ’²‹³

@SHOPCOMABLE_0
RESULTS = ’²‹³
IF IS_USEABLE_ATELIER()
	;’²‹³‘ÎÛ‚ª‚¢‚é‚È‚ç
	SIF TARGET < 0
		RETURN -1
	RETURN 1
ELSE
	RETURN 0
ENDIF

@SHOPCOM_0, ARG
CALL TRAIN
RETURN 1
