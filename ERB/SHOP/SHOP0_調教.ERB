@SHOPCOMNAME_0
RESULTS = 调教

@SHOPCOMABLE_0
RESULTS = 调教
IF IS_USEABLE_ATELIER()
	SIF TARGET < 0
		RETURN -1
	SIF !IS_TARGETABLE(TARGET)
		RETURN -1
	RETURN 1
ELSE
	RETURN 0
ENDIF

@SHOPCOM_0, ARG
CALL TRAIN
RETURN 1
