@SHOPCOMNAME_0
RESULTS = 调教

@SHOPCOMABLE_0
RESULTS = 调教
IF IS_USEABLE_ATELIER()
	SIF TARGET < 0
		RETURN -1
	SIF !IS_TARGETABLE(TARGET)
		RETURN -1
	SIF FLAG:调教引导 == 0 && FLAG:制造霍姆引导
		RETURN 4
	RETURN 1
ELSE
	RETURN 0
ENDIF

@SHOPCOM_0, ARG
IF FLAG:调教引导 == 0 && FLAG:制造霍姆引导
    FLAG:调教引导 = 1
	CALL SHOW_HELP_SINGLE, "调教", 0
ENDIF
CALL TRAIN
RETURN 1
