;------------------------------------------------------------------
;探索
;ワールドマップの表示や移動などの処理を行う
;------------------------------------------------------------------
@SHOPCOMNAME_10
RESULTS = 探索

@SHOPCOMABLE_10
RETURN 1

@SHOPCOM_10, ARG
;持ちだしたアイテムにフラグをつける
IF !ARG
	FOR LOCAL, GET_ITEM_RANGE("篮子"), GET_ITEM_RANGE("篮子", 1)
		CALL SET_ITEMDATA, LOCAL, "持ち出しフラグ", GET_ITEMDATA(LOCAL, "ID") > 0
	NEXT
ENDIF
CALL QUEST_MAP

