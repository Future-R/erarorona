;------------------------------------------------------------------
;ホムンクルスの作成処理
;------------------------------------------------------------------
@SHOPCOMNAME_21
RESULTS = 制造霍姆克鲁斯

@SHOPCOMABLE_21
SIF !IS_USEABLE_ATELIER()
	RETURN 0
SIF CMATCH(CFLAG:0:人类, 0) > 100
	RETURN 0
SIF !GET_EVENT_ATTAIN("采集吧！")
	RETURN -1
SIF FLAG:制造霍姆引导 == 0 && HAVE_ITEM("中和剂") > 1
	RETURN 4
RETURN 1

@SHOPCOM_21, ARG
IF FLAG:制造霍姆引导 == 0
	FLAG:制造霍姆引导 = 1
	CALL SHOW_HELP, "制造霍姆克鲁斯"
ENDIF
CALL CREATE_HOMU_MENU
