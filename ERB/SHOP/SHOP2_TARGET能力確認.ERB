@SHOPCOMNAME_2
RESULTS = TARGET能力確認

@SHOPCOMABLE_2
#LOCALSIZE 1
IF IS_USEABLE_ATELIER()
	RESULTS = TARGET能力確認
	SIF TARGET >= 0
		RETURN 2
ENDIF
RETURN 0

@SHOPCOM_2, ARG
CALL SHOW_CHARALIST, "能力確認", TARGET
