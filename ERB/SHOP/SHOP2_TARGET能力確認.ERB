@SHOPCOMNAME_2
RESULTS = TARGET查看能力

@SHOPCOMABLE_2
#LOCALSIZE 1
IF IS_USEABLE_ATELIER()
	RESULTS = TARGET查看能力
	SIF TARGET >= 0
		RETURN 2
ENDIF
RETURN 0

@SHOPCOM_2, ARG
CALL SHOW_CHARALIST, "查看能力", TARGET
