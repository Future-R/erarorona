;------------------------------------------------------------------
;主に料理などを使用する
;------------------------------------------------------------------
@SHOPCOMNAME_30
RESULTS = 使用物品

@SHOPCOMABLE_30
RESULTS = 使用物品
SIF !IS_USEABLE_ATELIER()
	RETURN 0
RETURN 1

@SHOPCOM_30, ARG
CALL SHOW_ITEMLIST, "使用物品", "持有物品"
