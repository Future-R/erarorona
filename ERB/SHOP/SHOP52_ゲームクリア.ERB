;==================================================
;エンディング後続行を選んだあとからの周回指定措置
;==================================================
@SHOPCOMNAME_52
RESULTS = 通关游戏

@SHOPCOMABLE_52
SIF !FLAG:结局指定
	RETURN 0
RETURN 1

@SHOPCOM_52, ARG
CALL TAKE_OVER
