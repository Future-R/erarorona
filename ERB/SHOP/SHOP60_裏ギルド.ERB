;------------------------------------------------------------------
;暗之公会
;------------------------------------------------------------------
@SHOPCOMNAME_60
RESULTS = 暗之公会

@SHOPCOMABLE_60
SIF GET_NAME("地点", FLAG:当前地点) != "亚兰德"
	RETURN 0
RETURN 1

@SHOPCOM_60, ARG
CALL MAIN_GUILD
