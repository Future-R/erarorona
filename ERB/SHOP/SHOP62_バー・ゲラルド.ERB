;------------------------------------------------------------------
;酒場
;依頼を受ける
;------------------------------------------------------------------
@SHOPCOMNAME_62
RESULTS = バー・ゲラルド

@SHOPCOMABLE_62
SIF GET_NAME("地点", FLAG:当前地点) != "阿兰亚村"
	RETURN 0
SIF !GET_EVENT_ATTAIN("柜台任务")
	RETURN 0
RETURN 1

@SHOPCOM_62, ARG
CALL MAIN_REQ, 1

