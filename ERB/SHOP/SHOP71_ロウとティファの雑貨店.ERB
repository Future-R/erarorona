;フルネームは長すぎるので杂货店として商店メニューに出す
@SHOPCOMNAME_71
RESULTS = 杂货店

@SHOPCOMABLE_71
RESULTS = 杂货店
SIF !GET_EVENT_ATTAIN("采集吧！")
	RETURN -1
SIF GET_NAME("地点", FLAG:当前地点) != "亚兰德"
	RETURN 0
RETURN 1

@SHOPCOM_71, ARG
CALL MAIN_STORE, GET_NO("贩售店", "罗与蒂法的杂货店")
RETURN 1
