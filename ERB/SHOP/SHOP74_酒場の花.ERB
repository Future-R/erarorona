@SHOPCOMNAME_74
RESULTS = 酒馆之花

@SHOPCOMABLE_74
RESULTS = 酒馆之花
SIF GET_NAME("地点", FLAG:当前地点) != "阿拉尼亚村"
	RETURN 0
SIF !GET_EVENT_ATTAIN("随波漂流")
	RETURN 0
; 周六折扣日，周日进货日
SIF WEEK(DAY) == 6
	RETURN 3
RETURN 1

@SHOPCOM_74, ARG
CALL MAIN_STORE, GET_NO("商店", "酒馆之花")
RETURN 1
