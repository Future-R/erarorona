;---------------------------------------------------------------------
;	Module		:EVENTLOAD.ERB
;	Facility	:
;
;	Licence		:フリー
;
;	Modification Data:セーブデータ読み込み時の処理
;
;
;	Edit	Date				Author					Reason
;	001		2012/06/10			kap						新規作成
;	001		2012/08/16			kap						バージョンアップ措置を追加
;---------------------------------------------------------------------
@EVENTLOAD
LOADGLOBAL
;オートセーブする場合の措置
SIF GLOBAL:自动保存的位置 > 0
	CALL SAVE_AUTO
CALL GAMESTART
;バージョンアップ措置
CALL VER_UP
CALL 初始化习惯特质系统
IF 习惯特质正在读档
	CALL 记录读档触发
	习惯特质正在读档 = 0
ENDIF
GETMILLISECOND
FLAG:游玩开始时间 = RESULT
IF FLAG:SHOP指令 > 0
	CALLFORM SHOPCOM_{FLAG:SHOP指令}, 1
	FLAG:SHOP指令 = 0
	RETURN 1
ENDIF
