;========================================================================
;セーブゲーム
;ページ数の記録には便宜性を考えてGLOBALを利用
;========================================================================
@TITLE_SAVEGAME
#LOCALSIZE 3
#LOCALSSIZE 1
#DIMS SAVE_TEXT, 2
#DIM LINE, 2
LOADGLOBAL
REDRAW 0
LINE = LINECOUNT
$START
CLEARLINE LINECOUNT - LINE
DRAWLINE
PRINTFORML 　SAVE GAME
DRAWLINE
FOR LOCAL, GLOBAL:保存数据页 * 10 , (GLOBAL:保存数据页) * 10 + 5
	VARSET SAVE_TEXT
	CHKDATA LOCAL
	SIF !RESULT
		SAVE_TEXT:0 = %RESULTS%
	CHKDATA LOCAL + 5
	SIF !RESULT
		SAVE_TEXT:1 = %RESULTS%
	FOR LOCAL:1, 0, 6
		FOR LOCAL:2, 0, 2
			SIF LOCAL + 5 * LOCAL:2 == GLOBAL:最终保存
				SETCOLOR  COLOR("p-blue") 
			SIF SAVE_TEXT:(LOCAL:2) == ""
				SETCOLOR  COLOR("gray") 
			SELECTCASE LOCAL:1
				CASE 0
					PRINT ┌
					PRINTBUTTON @"[{LOCAL + 5 * LOCAL:2, 2}]", @"{LOCAL + 5 * LOCAL:2}"
					PRINTFORM %"─"*25%┐
				CASE 1 TO 4
					PRINT │
					IF SAVE_TEXT:(LOCAL:2) == ""
						PRINTBUTTON  " " * 54 , @"{LOCAL + 5 * LOCAL:2}"
					ELSE
						PRINTBUTTON @"%AUTO_SPLIT(SAVE_TEXT:(LOCAL:2), "_", LOCAL:1 - 1), 54, LEFT%", @"{LOCAL + 5 * LOCAL:2}"
					ENDIF
					PRINT │
				CASE 5
					PRINTFORM └%"─"*27%┘
			ENDSELECT
			RESETCOLOR
		NEXT
		PRINTL 
	NEXT
NEXT
DRAWLINE
PRINTFORM 　%@"page.{GLOBAL:保存数据页}", 20, LEFT%
CALL PRINTSBUTTON_C, @"%"[-] 前のページ",32,LEFT%", "-", GLOBAL:保存数据页 > 0, , COLOR("gray")
PRINTBUTTON @"%"[/] 返回",32, LEFT%", "/"
CALL PRINTSBUTTON_C, @"%"[+] 次のページ",32,LEFT%", "+", GLOBAL:保存数据页 < 8, , COLOR("gray")
PRINTL 
DRAWLINE
LINE:1 = LINECOUNT
CALL EQUATE_LINE, LINE + MAXLINE()
INPUTS
IF ISNUMERIC(RESULTS)
	LOCAL =  TOINT(RESULTS)
	SIF !INRANGE(LOCAL, 0, 89)
		GOTO START
	CHKDATA LOCAL
	IF !RESULT
		CLEARLINE LINECOUNT - LINE:1
		PRINTL 数据已经存在。是否覆盖？
		CALL EQUATE_LINE, LINE + MAXLINE() - 2
		CALL INPUT_YN
		SIF RESULT == 1
			GOTO START
	ENDIF
	;変数の掃除
	CALL DEL_SAVE_VAR
	;最新のINFOを取るためにここで呼びだしてみる
	CALL SAVEINFO
	;SAVEDATA X, HOGEで、番号Xに、HOGEのてきすとをつけてセーブ
	GLOBAL:最终保存 = LOCAL
	SAVEGLOBAL
	SAVEDATA LOCAL, SAVESTR:セーブデータメモ
	CALL SHOW_BLANK
	RETURN 0
ELSE
	SELECTCASE RESULTS
		CASE "-"
			GLOBAL:保存数据页 = MAX(GLOBAL:保存数据页 - 1, 0)
			CALL SHOW_BLANK
		CASE "+"
			GLOBAL:保存数据页 = MIN(GLOBAL:保存数据页 + 1, 8)
			CALL SHOW_BLANK
		CASE "/"
			REDRAW 1
			SAVEGLOBAL
			RETURN 0
	ENDSELECT
ENDIF
GOTO START

@SAVEINFO
SAVESTR:セーブデータメモ =
SAVESTR:セーブデータメモ += @"%SHOW_DAY()% "
SAVESTR:セーブデータメモ += "_"

SAVESTR:セーブデータメモ += @"%CALLNAME:MASTER, 17, LEFT%"
SAVESTR:セーブデータメモ += @"MIXLv:{GET_MIXLV(MASTER),2}" + " " * 5
SAVESTR:セーブデータメモ += "Rank:"+GET_NAME("调教师等阶", FLAG:公会等阶)
SAVESTR:セーブデータメモ += "_"

IF GET_NO("商店", "探索") == FLAG:商店指令
	SAVESTR:セーブデータメモ +=@"%"所在地:" + GET_NAME("地点", FLAG:当前地点) + "(探索中)", 30, LEFT%"
ELSE
	SAVESTR:セーブデータメモ += @"%"所在地:工房", 30, LEFT%"
ENDIF
SAVESTR:セーブデータメモ += "资金:" + TOSTR(MONEY, "0克尔")
SAVESTR:セーブデータメモ += "_"
;SAVESTR:セーブデータメモ += "メモ:" + SAVESTR:セーブデータメモ
;SAVESTR:セーブデータメモ += "_"
SAVESTR:セーブデータメモ += "Ver" + TOSTR(GAMEBASE_VERSION / 1000) + "." +TOSTR(GAMEBASE_VERSION % 1000, "00") + " " * 4
GETMILLISECOND
FLAG:游玩时间 += RESULT - FLAG:游玩开始时间
FLAG:游玩开始时间 = RESULT
IF FLAG:游玩时间 / 1000 / 60 / 60 > 99
	SAVESTR:セーブデータメモ += "【99:59:59】"
ELSE
	SAVESTR:セーブデータメモ += "Play: " + TOSTR(FLAG:游玩时间 / 1000 / 60 / 60, "00") + ":" + TOSTR(FLAG:游玩时间 / 1000 / 60 % 60, "00") + ":" + TOSTR(FLAG:游玩时间 / 1000 % 60 % 60, "00") + " " * 4
ENDIF
GETTIME
SAVESTR:セーブデータメモ += "Last:" + RESULTS

@DEL_SAVE_VAR
;セーブデータサイズ削減処理
;変数は0が入っている場合、セーブデータのサイズが減るので
;一時変数に近いものは0にしたほうがよい
VARSET A
VARSET ITEMLIST
VARSET MIXLIST
VARSET MIXCHOICE
VARSET P_LIST
VARSET F
VARSET G
VARSET H
VARSET I
VARSET J
VARSET K
VARSET L
VARSET M
VARSET N
VARSET O
VARSET P
VARSET Q
VARSET R
VARSET S
VARSET T
VARSET U
VARSET V
VARSET W
VARSET X
VARSET Y
VARSET Z
VARSET TFLAG

REPEAT CHARANUM
	VARSET GOTJUEL:COUNT:0
	VARSET EX:COUNT:0
	VARSET PALAM:COUNT:0
	VARSET SOURCE:COUNT:0
	VARSET TCVAR:COUNT:0
REND

@SAVE_AUTO_START
SIF DAY == FLAG:自动保存实施日
	RETURN 0
FLAG:自动保存实施日 = DAY
;変数の掃除
CALL DEL_SAVE_VAR
CALL SAVEINFO
;一旦100番に記録しておく
SAVEDATA 100, SAVESTR:セーブデータメモ
;90-98までのセーブデータをずらす
;ただし、コピーが一度ロードを挟む必要があるのでGLOBALを渡して続行させる
FOR GLOBAL:自动保存的位置, 98, 89, -1
	SIF CHKDATA(GLOBAL:自动保存的位置)
		CONTINUE
	SAVEGLOBAL
	LOADDATA GLOBAL:自动保存的位置
NEXT
;オートセーブが存在しない場合、90番に移して終了
DELDATA 100
SAVEDATA 90, SAVESTR:セーブデータメモ

;ロード直後から呼ばれてくる地点
@SAVE_AUTO
IF GLOBAL:自动保存的位置 == 90
	SAVEDATA GLOBAL:自动保存的位置 + 1, SAVESTR:セーブデータメモ
	GLOBAL:自动保存的位置 = 100
	SAVEGLOBAL
	LOADDATA 100
ELSEIF GLOBAL:自动保存的位置 == 100
	DELDATA 100
	SAVEDATA 90, SAVESTR:セーブデータメモ
	GLOBAL:自动保存的位置 = 0
	SAVEGLOBAL
	;最後に通常セーブのロード処理に流すために呼び出す
	LOADDATA 90
ELSE
	SAVEDATA GLOBAL:自动保存的位置 + 1, SAVESTR:セーブデータメモ
	WHILE GLOBAL:自动保存的位置 >= 90
		GLOBAL:自动保存的位置--
		SAVEGLOBAL
		SIF !CHKDATA(GLOBAL:自动保存的位置)
			LOADDATA GLOBAL:自动保存的位置
	WEND
	IF !CHKDATA(100)
		GLOBAL:自动保存的位置 = 100
		SAVEGLOBAL
		LOADDATA 100
	ENDIF
	GLOBAL:自动保存的位置 = 0
ENDIF

;デフォルトでは問題あるので消去
@SYSTEM_AUTOSAVE
RETURN 0
