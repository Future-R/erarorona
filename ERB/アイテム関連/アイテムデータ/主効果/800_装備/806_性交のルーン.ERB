;---------------------------------------------------------------------------------------------------------
;	Edit	Date				Author					Reason								Ver
;	001		2012/10/09			kap						新規作成							0.410
;---------------------------------------------------------------------------------------------------------
;ARG		アイテムの登録番号
;ARG:1		使用対象
;ARG:2		使用者
;--------------------
@EFFECT_NAME_806
RESULTS = 性交符文

@EFFECT_EXPLAIN_806
RESULTS = 强化性交属性调教指令的效果。

;装備箇所・装備角色
@EFFECT_EQUIP_806, ARGS, ARG = -1
IF ARG == -1 && ARGS == ""
	RESULTS = 性交强化
	RETURN 30
ENDIF

@EFFECT_EQUIP_STATUS_806, ARGS, ARGS:1, ARG
SELECTCASE ARGS
	CASE "性交"
		RETURN CALCF_EQUIP_POWER(30, ARGS:1, ARG)
	CASEELSE
		RETURN 0
ENDSELECT
