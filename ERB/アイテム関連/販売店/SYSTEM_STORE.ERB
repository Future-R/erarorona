;---------------------------------------------------------------------
;
;	Module		:SYSTEM_STORE.ERB
;	Facility	:販売店関連の下位関数群
;
;	Licence		:フリー
;
;	Modification Data:販売店を動かすための関数
;
;
;	Edit	Date				Author					Reason
;	001		2012/07/15			kap						新規作成
;	002		2012/09/03			kap						アイテムが存在しない場合の抹消処理を追加
;														最大在庫数が減った場合のフォロー措置を追加
;	003		2012/09/19			kap						メッセージのクリック範囲拡張に対応
;	004		2012/09/29			kap						補正値の設定を追加
;	005		2012/11/14			kap						闇ギルドのアイテム購入でのエラー落ちを修正
;	006		2012/12/19			kap						オーナーの名前呼び出しが正しく行われていなかったのを修正
;	007		2013/04/01			kap						売却額の取得関数を追加
;---------------------------------------------------------------------
;@SET_STORE			店の基本商品の登録
;@RECHARGE_STORE	在庫補充関数
;@GET_SELL_PRICE	アイテムの価格取得関数
;----------------------------------------------------------------------

;-------------------------------------------------------------------------------
;@SET_STORE
;店の基本商品の登録, ERBに設定されている情報からアイテムを配列に入れる
;アイテム情報は登録品との絡みもあるので、通常のアイテムと同じようにDAにいれて保存する
;
;ARG	対象とする販売店番号、省略した場合は全店舗
;-------------------------------------------------------------------------------
@SET_STORE, ARG = -1
#DIM ITEMNUM
#DIM TEMP
#DIM ID
FOR LOCAL:1, (ARG == -1 ? 0 # ARG), (ARG == -1 ? 販売店数 # ARG + 1)
	FOR LOCAL, 0, 最大商品数
		ITEMNUM = GET_ITEM_RANGE("販売店") + LOCAL:1 * (最大商品数 + 最大登録数) + LOCAL
		TRYCCALLFORM STORE_SELL_{LOCAL:1}, LOCAL
			IF RESULT <= 0
				CALL DEL_ITEMDATA, ITEMNUM
				CONTINUE
			ENDIF
		CATCH
			CONTINUE
		ENDCATCH
		ID = RESULT
		CALL SET_ITEMDATA, ITEMNUM, "ID", RESULT
		CALL SET_ITEMDATA, ITEMNUM, "クオリティ", RESULT:1
		CALL SET_ITEMDATA, ITEMNUM, "品質", RESULT:2
		CALL SET_ITEMDATA, ITEMNUM, "価格", RESULT:3
		CALL SET_ITEMDATA, ITEMNUM, "量販価格", RESULT:3
		CALL SET_ITEMDATA, ITEMNUM, "最大在庫数", RESULT:4
		CALL SET_ITEMDATA, ITEMNUM, "在庫数", MIN(GET_ITEMDATA(ITEMNUM, "在庫数") ,RESULT:4)
		SPLIT RESULTS, "/", LOCALS
		FOR LOCAL:2, 0, RESULT
			SIF LOCALS:(LOCAL:2) == ""
				CONTINUE
			CALL SET_ITEMDATA, ITEMNUM, @"特性{LOCAL:2}", GET_NO("特性", LOCALS:(LOCAL:2))
		NEXT
		SPLIT RESULTS:1, "/", LOCALS
		FOR LOCAL:2, 0, RESULT
			SIF LOCALS:(LOCAL:2) == ""
				CONTINUE
			CALL SET_ITEMDATA, ITEMNUM, @"主効果{LOCAL:2}", GET_NO("主効果", LOCALS:(LOCAL:2))
		NEXT
		;使用回数の設定
		TRYCCALLFORM ITEM_COUNT_{ID}
			LOCAL:2 = RESULT
			CALL SET_ITEMDATA, ITEMNUM, "使用回数", LOCAL:2
			CALL SET_ITEMDATA, ITEMNUM, "使用回数上限", LOCAL:2
		CATCH
		ENDCATCH
		;カテゴリの設定
		CALL SET_CATEGORY, ITEMNUM
		;LVの設定
		CALLFORM ITEM_LV_{ID}
		CALL SET_ITEMDATA, ITEMNUM, "レベル", RESULT
		;補正値の設定
		CALL SET_REVICE, ITEMNUM
	NEXT
NEXT

;-----------------------------------------------------------------------
;@RECHARGE_STORE
;在庫補充関数
;-----------------------------------------------------------------------
@RECHARGE_STORE
#LOCALSIZE 3
FOR LOCAL:2, 0, 販売店数
	FOR LOCAL:1, 0, 最大商品数 + 最大登録数
		LOCAL = GET_ITEM_RANGE("販売店") + LOCAL:2 * (最大商品数 + 最大登録数) + LOCAL:1
		SIF MAX(GET_ITEMDATA(LOCAL, "最大在庫数"), GET_ITEMDATA(LOCAL, "在庫数")) <= GET_ITEMDATA(LOCAL, "在庫数")
			CONTINUE
		;補充情報もつけておく。また補充された場合、交友値があがる
		IF !GETBIT(FLAG:補充情報, LOCAL:2)
			CALLFORM STORE_OWNER_{LOCAL:2}
			SIF GET_CNUM(RESULTS) >= 0 && GET_FRIEND_POINT(RESULTS) < 60
				CALL GAIN_FRIEND_POINT, RESULTS, 100
			SETBIT FLAG:補充情報, LOCAL:2
		ENDIF
		CALL SET_ITEMDATA, LOCAL, "在庫数", MAX(GET_ITEMDATA(LOCAL, "最大在庫数"), GET_ITEMDATA(LOCAL, "在庫数"))
	NEXT
NEXT


;-----------------------------------------------------------------------
;@STORE_MESSAGE
;販売店メッセージ表示用関数
;
;ARG, 	登録店番号
;ARGS,	シチュエーション
;-----------------------------------------------------------------------
@STORE_MESSAGE, ARG, ARGS
#LOCALSSIZE 1
CALLFORM STORE_OWNER_{ARG}
LOCALS = %RESULTS%
IF GET_CNUM(LOCALS) >= 0
	;オーナーとの面識が出来る
	CFLAG:GET_CNUM(LOCALS):面識 = 1
ENDIF
CALLFORM STORE_MESSAGE_{ARG}, ARGS
SELECTCASE ARGS
	CASE "価格交渉確認"
		CALL MESSAGE_WINDOW, LOCALS, RESULTS
		CALL INPUT_YN_M, , "購入する", "購入しない"
		RETURN RESULT
	CASEELSE
		CALL MESSAGE_WINDOW, LOCALS, RESULTS
ENDSELECT

;-----------------------------------------------------------------------
;@GET_SELL_PRICE
;アイテムの価格取得関数
;
;ARG 売るアイテムの番号
;-----------------------------------------------------------------------
@GET_SELL_PRICE, ARG
#FUNCTION
#LOCALSIZE 1
LOCAL = 100
;素材
IF GET_ITEMDATA(ARG, "ID") < 500
	LOCAL += (GET_RECORD_ATTAIN("素材知識三級") > 0) * 20
	LOCAL += (GET_RECORD_ATTAIN("素材知識特級") > 0) * 20
ELSEIF GET_ITEMDATA(ARG, "ID") < 1600
	LOCAL += (GET_RECORD_ATTAIN("錬金知識三級") > 0) * 20
	LOCAL += (GET_RECORD_ATTAIN("錬金知識特級") > 0) * 20
ENDIF
LOCAL += FR_SKILL_LV:GET_NO("フレンドスキル", "商売人") * 20
RETURNF TIMESF(GET_ITEMDATA(ARG, "価格"), LOCAL)
