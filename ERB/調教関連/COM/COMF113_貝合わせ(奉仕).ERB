;-------------------------------------------------
;貝合わせ
;-------------------------------------------------
@COM_NAME_113
RESULTS = 磨豆腐(侍奉)

@COM_DOWNBASE_113
RETURN 80, 80

;@COM_JUMP_113, ARG, ARG:1, ARG:2
;IF HAVE_BODY("ペニス", ARG)
;	IF HAVE_BODY("ペニス", ARG:1)
;		RESULTS = 调教者にペニスがある/調教対象にペニスがある
;		RETURN GET_NO("指令", "兜合わせ")
;	ELSE
;		RESULTS = 调教者にペニスがない/調教対象にペニスがある
;		RETURN GET_NO("指令", "騎乗位素股")
;	ENDIF
;ELSE
;	IF HAVE_BODY("ペニス", ARG:1)
;		RESULTS = 调教者にペニスがある/調教対象にペニスがない
;		RETURN GET_NO("指令", "正常位素股")
;	ENDIF
;ENDIF
RETURN 0

@COM_LEARN_113, ARG
#FUNCTION
CALLFORMF COM_LEARN_{GET_NO("指令", "磨豆腐")}, ARG
RETURNF RESULT

@COM_ABLE_113, ARG, ARG:1, ARG:2
RESULTS = 调教者的腰可以使用/调教对象的阴道可以使用/调教者已充分润滑/调教对象已充分润滑
SIF !USE_WAIST(ARG:1)
	RETURN 0
SIF !USE_CLI(ARG)
	RETURN 0
SIF 润滑等级(ARG) < 3
	RETURN 0
SIF 润滑等级(ARG:1) < 3
	RETURN 0
RETURN 1

@COM_EFFECT_113
RESULTS = 对Ｃ快感(小)/受到Ｃ快感(小)

@COM_PROPERTY_113
RESULTS = 性交侍奉

@COM_SOURCE_113, ARGS, ARG = -1, ARG:1
SELECTCASE ARGS
	CASE "快Ｃ"
		RETURN 100, 125
	CASE "性行动"
		RETURN 150, 0
	CASE "屈从"
		RETURN 50, 0
	CASE "露出"
		RETURN 40
	CASE "逃脱"
		RETURN 500
	CASE "反感"
		RETURN 350
	CASEELSE
		RETURN 0
ENDSELECT
@COM_ACT_113, ARG, ARG:1, ARG:2

;-------------------------------------------------
;調教時テキスト
;-------------------------------------------------
@MESSAGE_COM_113, ARG, ARG:1, ARG:2
IF GET_ABL("侍奉精神", ARG) > 2
	RESULTS = %CALLNAME:ARG%得到了许可，热心地用自己性器摩擦着%CALLNAME:(ARG:1)%的性器。
ELSEIF GET_ABL("服从", ARG) > 2
	RESULTS = %CALLNAME:ARG%得到了许可，用自己性器摩擦着%CALLNAME:(ARG:1)%的性器。
ELSE
	RESULTS = %CALLNAME:ARG%在对方的命令下，战战兢兢地地用自己性器摩擦着%CALLNAME:(ARG:1)%的性器。
ENDIF
IF GET_ABL("欲望", ARG) > 3
	RESULTS += @"_%CALLNAME:ARG%在这个过程中逐渐兴奋起来，忘我地贪求着快感……"
ELSEIF GET_ABL("服从", ARG) > 2
	RESULTS += @"_%CALLNAME:ARG%感受到%CALLNAME:(ARG:1)%的呼吸逐渐粗重，于是自己也加快了腰间的动作……"
ENDIF

@COM_TOUCH_113
RESULTS = 阴道/阴道

@COM_STAIN_113

;EXPはRESULTがARG, RESULT:1がARG:1に加算される
@COM_EXP_113, ARGS, ARG = -1, ARG:1 = -1
SELECTCASE ARGS
	CASEELSE
		RETURN 0
ENDSELECT


@COM_ORDER_NUM_113
RETURN 20
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
@COM_ORDER_113, ARG, ARG:1
;IF TEQUIP:ARG:縄
;	CALL ADD_COM_ORDER, "判定無視", "緊縛"
;	RETURN 0
;ENDIF
CALL ADD_COM_ORDER, "ABL", "欲望", 3, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "Ｃ感觉", 2, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "性交成瘾", 1, ARG, ARG:1
CALL ADD_COM_ORDER, "MARK", "快乐烙印", 3, ARG, ARG:1
CALL ADD_COM_ORDER, "TALENT", "清纯", -2, ARG, ARG:1
;SIF !TALENT:ARG:淫乱 && !TALENT:ARG:娼妇
;	CALL ADD_COM_ORDER, "TALENT", "快感の否定", -5, ARG, ARG:1
;男嫌い（爱慕、亲密で無効）
;SIF HATE(ARG, ARG:1)
;	CALL ADD_COM_ORDER, "TALENT", \@IS_MALE(PLAYER) ? 男嫌い # 女嫌い \@, -12, ARG, ARG:1
;CALL ADD_COM_ORDER, "TALENT", "爱慕", 15, ARG, ARG:1
;CALL ADD_COM_ORDER, "TALENT", "亲密", 20, ARG, ARG:1
SIF 欲情等级(ARG) > 0
	CALL ADD_COM_ORDER, "EXTRA", "欲情", 欲情等级(ARG) * 3, ARG, ARG:1
;PALAM:润滑
SIF 润滑等级(ARG) > 4
	CALL ADD_COM_ORDER, "EXTRA", "充分润滑", 5, ARG, ARG:1
;CALL ADD_COM_ORDER, "EXTRA", "春药", 6, ARG, ARG:1
