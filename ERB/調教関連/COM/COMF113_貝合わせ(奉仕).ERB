;-------------------------------------------------
;貝合わせ
;-------------------------------------------------
@COM_NAME_113
RESULTS = 磨豆腐(侍奉)

@COM_DOWNBASE_113
RETURN 80, 80

;@COM_JUMP_113, ARG, ARG:1, ARG:2
;IF HAVE_BODY("阴茎", ARG)
;	IF HAVE_BODY("阴茎", ARG:1)
;		RESULTS = 調教者にペニスがある/調教対象にペニスがある
;		RETURN GET_NO("指令", "兜合わせ")
;	ELSE
;		RESULTS = 調教者にペニスがない/調教対象にペニスがある
;		RETURN GET_NO("指令", "騎乗位素股")
;	ENDIF
;ELSE
;	IF HAVE_BODY("阴茎", ARG:1)
;		RESULTS = 調教者にペニスがある/調教対象にペニスがない
;		RETURN GET_NO("指令", "正常位素股")
;	ENDIF
;ENDIF
RETURN 0

@COM_LEARN_113, ARG
#FUNCTION
CALLFORMF COM_LEARN_{GET_NO("指令", "磨豆腐")}, ARG
RETURNF RESULT

@COM_ABLE_113, ARG, ARG:1, ARG:2
RESULTS = 調教者が腰を使用可能/調教対象のヴァギナを使用可能/調教者の润滑が十分/調教対象の润滑が十分
SIF !USE_WAIST(ARG:1)
	RETURN 0
SIF !USE_CLI(ARG)
	RETURN 0
SIF 润滑等级(ARG) < 3
	RETURN 0
SIF 润滑等级(ARG:1) < 3
	RETURN 0
RETURN 1

@COM_EFFECT_113
RESULTS = Ｃに快感(小)/Ｃに快感を受ける(小)

@COM_PROPERTY_113
RESULTS = 性交奉仕

@COM_SOURCE_113, ARGS, ARG = -1, ARG:1
SELECTCASE ARGS
	CASE "快Ｃ"
		RETURN 100, 125
	CASE "性行动"
		RETURN 150, 0
	CASE "屈从"
		RETURN 50, 0
	CASE "露出"
		RETURN 40
	CASE "逃脱"
		RETURN 500
	CASE "反感"
		RETURN 350
	CASEELSE
		RETURN 0
ENDSELECT
@COM_ACT_113, ARG, ARG:1, ARG:2

;-------------------------------------------------
;調教時テキスト
;-------------------------------------------------
@MESSAGE_COM_113, ARG, ARG:1, ARG:2
IF GET_ABL("侍奉精神", ARG) > 2
	RESULTS = %CALLNAME:ARG%は許可を得ると、%CALLNAME:(ARG:1)%の性器に熱心に自分の性器を擦りつけた。
ELSEIF GET_ABL("服从", ARG) > 2
	RESULTS = %CALLNAME:ARG%は許可を得ると、%CALLNAME:(ARG:1)%の性器に自分の性器を擦りつけた。
ELSE
	RESULTS = %CALLNAME:ARG%は命じられると、おずおずと%CALLNAME:(ARG:1)%の性器に自分の性器を擦りつけた。
ENDIF
IF GET_ABL("欲望", ARG) > 3
	RESULTS += @"_%CALLNAME:ARG%は擦りつけているうちに興奮し、夢中になって快楽を貪っている…"
ELSEIF GET_ABL("服从", ARG) > 2
	RESULTS += @"_%CALLNAME:ARG%は%CALLNAME:(ARG:1)%が息を徐々に荒くするのを感じて腰の動きを激しくした…"
ENDIF

@COM_TOUCH_113
RESULTS = 阴道/阴道

@COM_STAIN_113

;EXPはRESULTがARG, RESULT:1がARG:1に加算される
@COM_EXP_113, ARGS, ARG = -1, ARG:1 = -1
SELECTCASE ARGS
	CASEELSE
		RETURN 0
ENDSELECT


@COM_ORDER_NUM_113
RETURN 20
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
@COM_ORDER_113, ARG, ARG:1
;IF TEQUIP:ARG:縄
;	CALL ADD_COM_ORDER, "判定無視", "緊縛"
;	RETURN 0
;ENDIF
CALL ADD_COM_ORDER, "ABL", "欲望", 3, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "Ｃ感觉", 2, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "性交成瘾", 1, ARG, ARG:1
CALL ADD_COM_ORDER, "MARK", "快乐烙印", 3, ARG, ARG:1
CALL ADD_COM_ORDER, "TALENT", "清纯", -2, ARG, ARG:1
;SIF !TALENT:ARG:淫乱 && !TALENT:ARG:娼妇
;	CALL ADD_COM_ORDER, "TALENT", "快感の否定", -5, ARG, ARG:1
;男嫌い（爱慕、亲密で無効）
;SIF HATE(ARG, ARG:1)
;	CALL ADD_COM_ORDER, "TALENT", \@IS_MALE(PLAYER) ? 男嫌い # 女嫌い \@, -12, ARG, ARG:1
;CALL ADD_COM_ORDER, "TALENT", "爱慕", 15, ARG, ARG:1
;CALL ADD_COM_ORDER, "TALENT", "亲密", 20, ARG, ARG:1
SIF 欲情等级(ARG) > 0
	CALL ADD_COM_ORDER, "EXTRA", "欲情", 欲情等级(ARG) * 3, ARG, ARG:1
;PALAM:润滑
SIF 润滑等级(ARG) > 4
	CALL ADD_COM_ORDER, "EXTRA", "润滑十分", 5, ARG, ARG:1
;CALL ADD_COM_ORDER, "EXTRA", "春药", 6, ARG, ARG:1
