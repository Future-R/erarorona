@COM_NAME_120
RESULTS = 胸揉み(奉仕)

@COM_EFFECT_120
RESULTS = Ｂに快感を受ける(小)

@COM_PROPERTY_120
RESULTS = 奉仕/手技

@COM_DOWNBASE_120
RETURN 30, 50

@COM_LEARN_120, ARG
#FUNCTION
RESULT = 2
RETURNF 1

@COM_ABLE_120, ARG, ARG:1, ARG:2
RESULTS = 調教対象が手を使用可能/調教者の胸を使用可能
SIF !USE_HAND(ARG)
	RETURN 0
SIF !USE_BUST(ARG:1)
	RETURN 0
RETURN 1


@COM_SOURCE_120, ARGS, ARG = -1, ARG:1
SELECTCASE ARGS
	CASE "快Ｂ"
		RETURN 0, 150
	CASE "性行動"
		RETURN 30
	CASE "露出"
		RETURN 20
	CASE "性行動"
		RETURN 30
	CASE "屈従"
		RETURN 30, 0
	CASE "逸脱"
		RETURN 40
	CASE "反感"
		RETURN 40
	CASEELSE
		RETURN 0
ENDSELECT

@MESSAGE_COM_120, ARG, ARG:1, ARG:2
RESULTS = %CALLNAME:(ARG:1)%が
IF GET_TALENT("貧乳", ARG:1)
	IF GET_TALENT("小柄", ARG:1)
		RESULTS += "僅かに膨らみが感じられる程度の"
	ELSE
		RESULTS += "小振りの"
	ENDIF
ENDIF

IF GET_TALENT("巨乳", ARG:1)
	IF GET_TALENT("長身", ARG:1)
		RESULTS += "手に余るほど大きな"
	ELSE
		RESULTS += "手から溢れそうなほどの"
	ENDIF
ENDIF
RESULTS += "胸を差し出すと_" + CALLNAME:ARG + "は"
;母乳体質
;IF TALENT:149
	;欲情のPALAMLV3以上＋恋慕or親愛
;	IF PALAM:6 > PALAMLV:3 && (TALENT:3 || TALENT:6)
;		PRINTFORMW 胸を強く搾り上げてやると、%CALLNAME:TARGET%は嬌声をあげて、より強い刺激を求めた……
	;欲情のPALAMLV3以上
;	ELSEIF PALAM:6 > PALAMLV:3
;		PRINTW 胸を強く搾り上げた……
;	ELSE
;		PRINTW 胸を搾り上げた……
;	ENDIF
;ELSE
	IF GET_TALENT("貧乳", ARG:1)
		IF GET_ABL("奉仕精神", ARG) > 1
			IF 欲情レベル(ARG) > 3
				RESULTS += @"頬を上気させ、嬉しそうに胸をまさぐってきた……"
			ELSE
				RESULTS += @"%CALLNAME:ARG%の反応を確かめながら優しく撫で回してきた…"
			ENDIF
		ELSE
			RESULTS += @"恐る恐る手を伸ばすと、胸を優しく撫でてきた…"
		ENDIF
	ELSE
		IF GET_ABL("奉仕精神", ARG) > 1
			IF 欲情レベル(ARG) > 3
				RESULTS += @"夢中になって胸を揉みしだいた…"
			ELSE
				RESULTS += @"痛くならないように注意しながら胸を感触を楽しみはじめた…"
			ENDIF
		ELSE
			RESULTS += @"恐る恐る手を伸ばすと、胸を揉みしだきはじめた…"
		ENDIF
	ENDIF
;ENDIF
@COM_TOUCH_120
RESULTS = 手/胸
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
@COM_ORDER_120, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "欲望", 1, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "奉仕精神", 4, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "精液中毒", 1, ARG, ARG:1
CALL ADD_COM_ORDER, "MARK", "快楽刻印", 1, ARG, ARG:1
;CALL ADD_COM_ORDER, "PALAM", "欲情", 1, ARG, ARG:1
CALL ADD_COM_ORDER, "TALENT", "清純", -1, ARG, ARG:1
CALL ADD_COM_ORDER, "TALENT", "献身的", 6, ARG, ARG:1

;-------------------------------------------------
;実行値
;-------------------------------------------------
@COM_ORDER_NUM_120
RETURN 18
