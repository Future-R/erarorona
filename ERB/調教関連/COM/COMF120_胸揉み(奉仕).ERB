@COM_NAME_120
RESULTS = 揉胸(侍奉)

@COM_EFFECT_120
RESULTS = 受到Ｂ快感(小)

@COM_PROPERTY_120
RESULTS = 侍奉/手活

@COM_DOWNBASE_120
RETURN 30, 50

@COM_LEARN_120, ARG
#FUNCTION
RESULT = 2
RETURNF 1

@COM_ABLE_120, ARG, ARG:1, ARG:2
RESULTS = 调教对象的手可以使用/调教者的胸可以使用
SIF !USE_HAND(ARG)
	RETURN 0
SIF !USE_BUST(ARG:1)
	RETURN 0
RETURN 1


@COM_SOURCE_120, ARGS, ARG = -1, ARG:1
SELECTCASE ARGS
	CASE "快Ｂ"
		RETURN 0, 150
	CASE "性行动"
		RETURN 30
	CASE "露出"
		RETURN 20
	CASE "性行动"
		RETURN 30
	CASE "屈从"
		RETURN 30, 0
	CASE "逃脱"
		RETURN 40
	CASE "反感"
		RETURN 40
	CASEELSE
		RETURN 0
ENDSELECT

@MESSAGE_COM_120, ARG, ARG:1, ARG:2
RESULTS = %CALLNAME:(ARG:1)%挺身递出
IF GET_TALENT("贫乳", ARG:1)
	IF GET_TALENT("娇小", ARG:1)
		RESULTS += "只能感受到些许鼓起的"
	ELSE
		RESULTS += "小小的"
	ENDIF
ENDIF

IF GET_TALENT("巨乳", ARG:1)
	IF GET_TALENT("高挑", ARG:1)
		RESULTS += "一只手把握不住的"
	ELSE
		RESULTS += "会从手掌间满溢而出的"
	ENDIF
ENDIF
RESULTS += "胸部_" + CALLNAME:ARG + "则"
;母乳体質
;IF TALENT:149
	;欲情のPALAMLV3以上＋爱慕or亲密
;	IF PALAM:6 > PALAMLV:3 && (TALENT:3 || TALENT:6)
;		PRINTFORMW 胸を強く搾り上げてやると、%CALLNAME:TARGET%は嬌声をあげて、より強い刺激を求めた……
	;欲情のPALAMLV3以上
;	ELSEIF PALAM:6 > PALAMLV:3
;		PRINTW 胸を強く搾り上げた……
;	ELSE
;		PRINTW 胸を搾り上げた……
;	ENDIF
;ELSE
	IF GET_TALENT("贫乳", ARG:1)
		IF GET_ABL("侍奉精神", ARG) > 1
			IF 欲情等级(ARG) > 3
				RESULTS += @"脸颊泛红，开心地揉捏起对方的胸部……"
			ELSE
				RESULTS += @"感受着对方的反应，同时温柔地反复抚摸胸部……"
			ENDIF
		ELSE
			RESULTS += @"胆怯地伸出手，温柔抚摸着对方的胸部……"
		ENDIF
	ELSE
		IF GET_ABL("侍奉精神", ARG) > 1
			IF 欲情等级(ARG) > 3
				RESULTS += @"忘我地揉捏着对方的胸部……"
			ELSE
				RESULTS += @"在小心不要弄疼对方的同时，开始享受起胸部的触感……"
			ENDIF
		ELSE
			RESULTS += @"胆怯地伸出手，开始揉捏起对方的胸部……"
		ENDIF
	ENDIF
;ENDIF
@COM_TOUCH_120
RESULTS = 手/胸
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
@COM_ORDER_120, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "欲望", 1, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "侍奉精神", 4, ARG, ARG:1
CALL ADD_COM_ORDER, "ABL", "精液成瘾", 1, ARG, ARG:1
CALL ADD_COM_ORDER, "MARK", "快乐烙印", 1, ARG, ARG:1
;CALL ADD_COM_ORDER, "PALAM", "欲情", 1, ARG, ARG:1
CALL ADD_COM_ORDER, "TALENT", "清纯", -1, ARG, ARG:1
CALL ADD_COM_ORDER, "TALENT", "献身的", 6, ARG, ARG:1

;-------------------------------------------------
;実行值
;-------------------------------------------------
@COM_ORDER_NUM_120
RETURN 18
